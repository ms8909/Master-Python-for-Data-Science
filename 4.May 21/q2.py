def rectangle(length, width):

  for i in range(length):
    print('*' * width)

flag_length = input('What is the length of the flag? = ')
flag_width = input('What is the width of the flag? = ')

flagpole_length = input('What is the length of the flagpole? = ')
flagpole_width = input('What is the length of the flagpole? = ')

rectangle(5, 10)
rectangle(6, 2)


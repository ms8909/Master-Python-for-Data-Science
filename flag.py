def flag(length, width):

  for i in range(length):
    print('*' * width)

flag_length = input(' length of the flag = ')
flag_width = input('width of the flag = ')

flagpole_length = input(' length of the flagpole = ')
flagpole_width = input(' length of the flagpole = ')

flag(5, 10)
flag(6, 2)

